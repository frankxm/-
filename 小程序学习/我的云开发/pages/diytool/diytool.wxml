<!-- 父组件向子组件传递数据tabs -->
<tabs tabs="{{tabs}}" binditemChange="change">
<block wx:if="{{tabs[0].isActive}}">0</block>
<block wx:elif="{{tabs[1].isActive}}">1</block>
<block wx:elif="{{tabs[2].isActive}}">2</block>
<block wx:else>3</block>
</tabs>
