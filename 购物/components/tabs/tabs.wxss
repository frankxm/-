.tabs .tabs_title {
  display: flex;
}
.tabs .tabs_title .title_item {
  align-items: center;
  justify-content: center;
  display: flex;
  flex: 1;
  padding: 15rpx 0;
}
.tabs .tabs_title .active {
  color: var(--themeColor);
  border-bottom: currentColor solid 5rpx;
}
